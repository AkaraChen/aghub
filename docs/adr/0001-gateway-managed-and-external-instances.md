# 网关 v1 同时支持托管实例与外部实例

CLIProxyAPI 有大量自建存量用户(42k+ stars),「连接外部实例」是他们进入 aghub 的入口,因此 v1 不收窄为只托管本地。代价是双状态机:托管实例有供给/启停/升级,外部实例只有连接测试;能力差异表达在类型上。外部实例无法发起 OAuth(回调只到发起登录的本机),账号通过本机登录后经 `/auth-files` 推送(账号漫游),不做授权中转。
