# PostHog credentials. The Rust binary reads these at compile time and
# exposes a typed app config to the webview via the synchronous
# get_app_config command. Copy this file to .env and fill in your
# project key. When unset, analytics + logs become silent no-ops.
POSTHOG_KEY=
POSTHOG_HOST=https://us.i.posthog.com
